<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="al_view_res_partner_inherit_params" model="ir.ui.view">
        <field name="name">al.view.res.partner.inherit.params</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <xpath expr="//group[@name='sale']/field[@name='user_id']" position="before">
                <field name="area_id" options="{'no_open': True}"/>
            </xpath>
            <xpath expr="//group[@name='sale']" position="inside">
                <field name="delivery_mode_id" widget="selection"/>
                <field name="payment_mode_id" widget="selection"/>
                <field name="partner_type_id" widget="selection"/>
                <field name="home_consumption" widget="boolean"/>
                <field name="out_home_consumption" widget="boolean"/>
            </xpath>

        </field>
    </record>

    <!--  action to open delivery.mode recordset  -->
    <record id="al_delivery_mode_window_action" model="ir.actions.act_window">
        <field name="name">Delivery Mode</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">delivery.mode</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!--  action to open Payment.mode recordset  -->
    <record id="al_payment_mode_window_action" model="ir.actions.act_window">
        <field name="name">Payment Mode</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">payment.mode</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!--  action to open res.partner.type recordset  -->
    <record id="al_partner_type_window_action" model="ir.actions.act_window">
        <field name="name">Partner Type</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.partner.type</field>
        <field name="view_mode">tree,form</field>
    </record>

</odoo>